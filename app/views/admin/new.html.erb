<h1>Editing <%= @model.to_s.titleize %></h1>

<%= link_to 'Back', things_path(@model) %>

<%= form_for([:admin, @thing]) do |form| %>
  <%= render 'form', f: form %>
  <%= form.button :submit %>
<% end %>
